/** No route intercepted the @modal slot for this navigation — render nothing. Required in this Next version: an unmatched named slot with no default.tsx errors instead of silently 404ing. */
export default function Default() {
  return null;
}
